VanillaTilt.init(document.querySelectorAll(".card"), {
    max: 25,
    speed: 400,
    glare: true,
    "max-glare": 1,
});

VanillaTilt.init(document.querySelectorAll(".hover"), {
    max: 10,
    speed: 400,
});

VanillaTilt.init(document.querySelectorAll(".servicio"), {
    max: 25,
    speed: 400,
});